<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/LinearLayoutListSeasons"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/opaquecentered"
    tools:context=".MainActivity" >
    <include
        android:id="@+id/toolbarSeason"
        layout="@layout/toolbar"/>
    <ProgressBar
        style="@style/ProgressBar.Spinner.Indeterminate.Large"
        android:layout_centerInParent="true"
        android:layout_gravity="center"
        android:indeterminate="true"
        android:id="@+id/progressbar_loading_seasons"
        android:visibility="gone"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
    <ExpandableListView
        android:id="@+id/listSeasonsExpandable"
        android:layout_below="@+id/toolbarSeason"
        android:paddingRight="13dp"
        android:paddingLeft="13dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" >
    </ExpandableListView>

</RelativeLayout>